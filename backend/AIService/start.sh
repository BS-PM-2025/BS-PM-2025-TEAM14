#!/bin/bash

# Install dependencies
npm install

# Run the service 
# We're not actually using this directly, but it's useful for testing
echo "AI Service is ready for use by the main FastAPI application!"
echo "To test it directly, run: node test.js" 